<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=pageName%></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <style>
        body{
            display: flex;
            flex-wrap: wrap;
        }
        .cardDiv{
            width: 17%;
            border: solid red;
            margin: 2px 1.5% 0 1.5%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .cardDiv>div{
            display: flex;
            align-items: center;
            flex-direction: column;
        }
        a{
            text-decoration: none;
            color: black;
        }
        a:hover{
            color: rgb(94, 94, 94);
        }
        .content{
            height: 100px;
            overflow:hidden;
        }
    </style>
    <% postList.forEach((element, index) => { %>
        <div class="cardDiv">
            <a href="/post<%=element.id%>">
                <img src="../image/<%= element.image%>" alt="element.image" class="card-img-top" style="width: 200px !important;">
                    <div class="card-body">
                        <div class="card-title"><%= element.title %></div>
                        <div class="card-text content"><%= element.content %></div>
                        <%if(admin=='true'){%>
            </a>
                         <a class="btn btn-primary" href="/edit?id=<%=element.id%>">Edit</a>
                    <%}%>
            </div>
        </div>
      <% }); %>